git -C ~/.config diff        # what you've changed since the last commit
git -C ~/.config log -p      # full history of a config change
git -C ~/.config checkout .  # throw away an experiment that broke things
